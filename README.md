# TP HADOOP HDSF

voir rendu dans python_jobs


> marche pour ARM64 : MAC m2 peux etre pas les autres systems
pour lancer le container :

```
cd docker-hadoop
docker swarm init
docker stack deploy -c docker-compose.yml hadoop
docker ps
```

